# Test Summary

The executable suite contains 69 deterministic tests covering the core state machine, timing arithmetic and rollover, input gates and recoverable/terminal exit states, variants, latest-event undo, recording/GPS-distance state, persistence, one-shot automatic completion, and validated layout-family routing. The authoritative commands and manual release checks are maintained in `docs/testing.md`; this file intentionally avoids duplicating per-test prose that can drift from the source.
