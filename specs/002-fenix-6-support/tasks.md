# Tasks: fenix-6-support

**Input**: Design documents from `specs/002-fenix-6-support/`
**Prerequisites**: plan.md (required), spec.md (required), research.md, quickstart.md

## Phase 1: Setup (Shared Infrastructure)

- [ ] T001 Create device validation script `scripts/validate-fenix6.sh` that builds the app, installs the .prg to a connected fenix 6 (or simulator), runs a 30-minute validation flow, and copies FIT/log output to `specs/002-fenix-6-support/validation-logs/`
- [ ] T002 [P] Create SBOM generation script `scripts/generate-sbom.sh` that outputs `sbom/sbom.json` (use syft/spdx or the team's preferred tool)
- [ ] T003 [P] Create CVE scan script `scripts/scan-cves.sh` that runs dependency CVE checks and writes results to `specs/002-fenix-6-support/cve-scan/`
- [ ] T004 Create `docs/RELEASE_CHECKLIST.md` containing fenix 6 validation and signing steps (include manifest & monkey.jungle checks)
- [ ] T005 Create `SECURITY.md` summarizing minimal security/privacy expectations and links to constitution sections

## Phase 2: Foundational (Blocking Prerequisites)

- [ ] T006 Update Connect IQ resource layout `resources/layouts/layout.xml` to ensure the main countdown timer is prominent and text is legible on fenix 6 (no manual pixel offsets; prefer scalable layout units)
- [ ] T007 Update `resources/strings/strings.xml` to verify concise, watch-scale strings and readable labels for half, timer, and card indicators
- [ ] T008 Review and, if needed, update `manifest.xml` and `monkey.jungle` to ensure fenix 6 target/device flags and activity recording permissions are present
- [ ] T009 Add simulator/unit checks under `tests/` (create `tests/timer_sync.mc` and `tests/haptics.mc`) that exercise timer synchronization and haptic triggers on simulator profiles
- [ ] T010 Run SBOM & CVE scan (invoke `scripts/generate-sbom.sh` and `scripts/scan-cves.sh`) and place results in `specs/002-fenix-6-support/cve-scan/`

## Phase 3: User Story 1 - Run match on fenix 6 (Priority: P1)

**Goal**: Ensure a referee can run a full match on fenix 6 with synchronized timers, distinct haptics, and valid FIT recording.

**Independent Test**: Run `scripts/validate-fenix6.sh` end-to-end and verify outputs in `specs/002-fenix-6-support/validation-logs/` and `validation-results.md`

- [ ] T011 [US1] Update `source/RugbyTimerView.mc` to bind UI state to `resources/layouts/layout.xml` and remove any imperative layout drawing that causes overflow on fenix 6
- [ ] T012 [US1] Update `source/RugbyHaptics.mc` to ensure distinct haptic patterns are used for card expiry and conversion expiry, and document thresholds in `docs/haptics.md`
- [ ] T013 [US1] Update `source/RugbyActivityRecorder.mc` to prefer `Activity.SPORT_RUGBY` when available and implement/test documented fallback mapping when SDK/device lacks the constant
- [ ] T014 [US1] Run device validation: execute `scripts/validate-fenix6.sh` and copy outputs to `specs/002-fenix-6-support/validation-logs/` (Owner: device owner)
- [ ] T015 [US1] Analyze validation logs and FIT files; produce `specs/002-fenix-6-support/validation-results.md` with pass/fail evidence and a prioritized list of issues
- [ ] T016 [US1] For each issue in `validation-results.md`, create a specific fix task (e.g., update resource, fix race) and implement fixes. Record fixes and re-run `scripts/validate-fenix6.sh` until green
- [ ] T017 [US1] Add/Update regression checks in `tests/` to cover any fixes (e.g., update `tests/timer_sync.mc`) and ensure they run in CI/simulator

## Phase 4: Polish & Cross-Cutting Concerns

- [ ] T018 Update `README.md` and `specs/002-fenix-6-support/quickstart.md` to include fenix 6 validation steps and where to find validation artifacts
- [ ] T019 [P] Clean up code and resources: refactor minor duplication introduced during fixes, update comments in changed files (files: `source/*`, `resources/*`)
- [ ] T020 Create or update `docs/compatibility.md` listing supported devices (fenix 6 as validated) and known limitations
- [ ] T021 Create a release candidate tag after validation: run `git tag -a v1.3.0-fenix6 -m "fenix-6 validation"` and push tags (owner responsibility)

## Dependencies & Execution Order

- Phase 1 tasks prepare tooling and docs and can run in parallel where marked [P]
- Phase 2 must complete (resource/manifest updates, tests, SBOM/CVE) before implementing user-story fixes
- Phase 3 (US1) depends on Phase 2 foundational tasks
- Phase 4 is final polishing after all validation is green

## Parallel Opportunities

- T002, T003, T019 are parallelizable
- Many documentation tasks (T004, T005, T018, T020) can run in parallel with code work

## Suggested MVP

- MVP: Complete Phase 1 + Phase 2 + T011..T015 (Run validation and produce validation-results.md). If green, stop and release a candidate.

---

(Generated by speckit.tasks)
