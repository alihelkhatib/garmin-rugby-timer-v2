# Implementation Plan: idle-timer-controls

**Branch**: 003-idle-timer-controls | **Date**: 2026-04-13 | **Spec**: specs/003-idle-timer-controls/spec.md

## Summary

Small behavioral change to route physical button inputs differently depending on active match state:
- Idle (no active match): Up/Menu increments main timer by 1 minute; Down decrements by 1 minute; Score dialog disabled.
- Active match: Existing in-match mappings (including score dialog) remain unchanged.

## Technical Context

**Language/Version**: Monkey C / Garmin Connect IQ SDK (specify exact SDK in implementation)  
**Dependencies**: WatchUi, System input handling, RugbyGameModel, RugbyTimerView, RugbyTimerDelegate, RugbyHaptics  
**Testing**: Simulator checks and physical device verification (fenix 6 recommended)

## Constitution Check

- Referee-first scope: preserves referee-first UI priority
- Synchronized timebase: unaffected (pre-match adjustments change initial countdown only)
- Simplicity/DRY: small targeted behavior change
- Declarative UI resources: no required layout changes (but verify if layout needs adjustment after displaying adjusted values)
- Garmin compatibility: test on representative devices
- Regression isolation: include regression tests for in-match button behavior
- Security/Privacy: N/A

## Phase 0: Research

- R-001 Inspect input handling in `source/RugbyTimerDelegate.mc` to find where Up/Menu and Down are bound to commands and which state gate (match active/inactive) is used.
- R-002 Identify any existing long-press handlers that could conflict with short-press mappings.

Deliverable: specs/003-idle-timer-controls/research.md

## Phase 1: Design

- D-001 Decide state gate: add explicit `isMatchActive()` check in the button handler before dispatching to score-menu vs timer-adjust logic.
- D-002 Confirm timer adjustment behavior: step = 60 seconds; ensure visual bounds (min 0, max variant half length if applicable).
- D-003 Define regression tests: update `tests/timer_sync.mc` and create `tests/idle_button_behavior.mc` to assert idle vs active mapping differences.

Deliverable: data-model.md (minimal), quickstart.md for manual test steps

## Phase 2: Implementation Tasks

- T001 Inspect `source/RugbyTimerDelegate.mc` for physical button mappings and add idle-state checks (file: source/RugbyTimerDelegate.mc)
- T002 Implement pre-match timer increment/decrement handlers in `source/RugbyGameModel.mc` or `source/RugbyTimerView.mc` as appropriate; ensure they update the displayed timer immediately (files: source/RugbyGameModel.mc, source/RugbyTimerView.mc)
- T003 Ensure `source/RugbyHaptics.mc` optionally provides short confirmation haptics for adjustment (optional)
- T004 Update tests: add `tests/idle_button_behavior.mc` and update `tests/timer_sync.mc` to include pre-match adjustment behavior
- T005 Run simulator and device validation: follow `specs/003-idle-timer-controls/quickstart.md` to validate behavior on fenix 6 and other representative devices
- T006 Add regression checks to CI pipeline (if present) to include new tests

## Phase 3: Polish & Cross-Cutting

- P-001 Update README and docs to mention pre-match adjustment feature
- P-002 Verify no other button flows were affected; run full regression tests

## Owner

Feature Owner: (you) — confirm UX behavior and run physical-device validation. Developer: repo maintainer to implement changes.

---
(Generated by speckit.plan)